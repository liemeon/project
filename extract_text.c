#include <stdio.h>
#include <sys/types.h>
#include <fcntl.h>
#include <stdlib.h>
#include <unistd.h>

int main(int argc, char *argv[]) // need argv only one;;
{
  if(argc != 2)
  {
    printf("extract original_file_name");
    exit(0);
  }
  if()//file_exist_check
  {

  }
  if()//text file exist check
  {

  }
  //system(""); //extract text

  return 0;
}
